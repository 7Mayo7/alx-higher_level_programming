#!/usr/bin/python3
def element_at(my_list, idx):
    idx = my_list.index()
    length = len(idx)
    if idx < 0:
        return None
    elif idx >= length:
        return None
    else:
        return("idx")
